{
    const calculateAge = (BIRTHDAY) => {
        console.log(Date.now());
        return this.BIRTHDAY;
    }

    const BIRTHDAY = "24/07/2002";
    let age = calculateAge(BIRTHDAY);``
}

console.log(age);